name: Validate

on:
  push:
    branches:
      - master
      - 'dev/**'
      - community
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      should-run-all: ${{ steps.check-types.outputs.should-run-all }}
      gui: ${{ steps.changes.outputs.gui }}
      vm: ${{ steps.changes.outputs.vm }}
      block: ${{ steps.changes.outputs.block }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: 'yarn'

      - name: Check types
        id: check-types
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            if [[ "${{ github.base_ref }}" == "master" ]]; then
              echo "should-run-all=true" >> $GITHUB_OUTPUT
            else
              echo "should-run-all=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "should-run-all=true" >> $GITHUB_OUTPUT
          fi

      - name: Detect changes
        uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: .github/paths-filter.yml

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            node-modules-${{ runner.os }}-

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build all packages
        run: yarn run build:full

      - name: Cache build artifacts
        uses: actions/cache/save@v4
        with:
          path: |
            packages/*/dist
            packages/*/build
            packages/*/playground
            packages/audio/dist.js
            packages/gui/static/microbit
          key: build-artifacts-${{ github.run_id }}-${{ github.run_attempt }}

  test-gui:
    name: Test GUI
    needs: [build]
    runs-on: ubuntu-latest
    if: needs.build.outputs.gui == 'true' || needs.build.outputs.should-run-all == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: 'yarn'

      - name: Restore node_modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            node-modules-${{ runner.os }}-

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Restore build artifacts
        uses: actions/cache/restore@v4
        with:
          path: |
            packages/*/dist
            packages/*/build
            packages/*/playground
            packages/audio/dist.js
            packages/gui/static/microbit
          key: build-artifacts-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Test
        run: yarn gui test:unit

  test-vm:
    name: Test VM
    needs: [build]
    runs-on: ubuntu-latest
    if: needs.build.outputs.vm == 'true' || needs.build.outputs.should-run-all == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: 'yarn'

      - name: Restore node_modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            node-modules-${{ runner.os }}-

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Restore build artifacts
        uses: actions/cache/restore@v4
        with:
          path: |
            packages/*/dist
            packages/*/build
            packages/*/playground
            packages/audio/dist.js
            packages/gui/static/microbit
          key: build-artifacts-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Test
        run: yarn vm test

  test-block:
    name: Test Block
    needs: [build]
    runs-on: ubuntu-latest
    if: needs.build.outputs.block == 'true' || needs.build.outputs.should-run-all == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: 'yarn'

      - name: Restore node_modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            node-modules-${{ runner.os }}-

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Restore build artifacts
        uses: actions/cache/restore@v4
        with:
          path: |
            packages/*/dist
            packages/*/build
            packages/*/playground
            packages/audio/dist.js
            packages/gui/static/microbit
          key: build-artifacts-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Test
        run: yarn block test
