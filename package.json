{
  "name": "monorepo",
  "description": "ClipCC's Workspace",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/google-closure-library",
      "**/google-closure-compiler",
      "**/google-closure-cmd"
    ]
  },
  "scripts": {
    "start": "yarn gui start",
    "prepare": "husky install",
    "build:dist": "NODE_ENV=production yarn build:full",
    "build:full": "yarn l10n build && yarn storage build && yarn render build && yarn block build && yarn vm build && yarn paint build && yarn gui build",
    "build": "yarn block build && yarn gui build",
    "test": "yarn gui test:unit && yarn vm tap",
    "translation:build": "yarn gui i18n:src && yarn vm i18n:src && yarn block translate",
    "translation:push": "tx push",
    "translation:pull": "yarn block translate:sync:translations && yarn l10n pull:editor",
    "gui": "yarn workspace scratch-gui",
    "vm": "yarn workspace scratch-vm",
    "block": "yarn workspace scratch-blocks",
    "l10n": "yarn workspace scratch-l10n",
    "render": "yarn workspace scratch-render",
    "storage": "yarn workspace scratch-storage",
    "paint": "yarn workspace scratch-paint",
    "parser": "yarn workspace scratch-parser"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "commitlint-config-gitmoji": "^2.2.10",
    "husky": "^8.0.3"
  }
}
